/*
 *  Problem: Implement Pow(x, n)
 *  Compile: g++ -o uniquePaths uniquePaths.cpp -std=c++11 && ./uniquePaths
 *  Execute: ./uniquePaths
 *  (c) Mohammad Mofrad, 2018
 *  (e) m.hasanzadeh.mofrad@gmail.com
 */

#include <iostream>
#include <stdint.h>
#include <stdio.h>
#include <vector>
#include <stdlib.h>
#include <utility>
#include <algorithm>
#include <string.h>

int uniquePaths(int m, int n) 
{
    int num_pathes = 0;
    return(num_pathes);
}

int main(int argc, char **argv)
{
    int m = 3, n = 2;
    printf("uniquePaths(%d, %d)=%d\n", m, n, uniquePaths(m, n));
    return(0);
}